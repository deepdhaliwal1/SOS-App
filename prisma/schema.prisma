generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model UserPhone {
  id     Int    @id @default(autoincrement())
  userId String @unique
  phone  String
}
